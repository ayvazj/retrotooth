package retrotooth;

import java.io.IOException;

final class BleResponseConverter implements Converter<RequestData, ResponseData> {
    @Override
    public ResponseData convert(RequestData value) throws IOException {
        return null;
    }
}
